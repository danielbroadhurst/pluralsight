/**
 * Node != Javascript
 * - Node VM V8 - Comes with Feature Groups Shipping/Stage/In Progress
 * - Node combines core modules, C++ Bindings, libuv and standard modules like http/fs
 */

// node - command would start a REPL
//      - tab tab will trigger auto complete
//      - require('repl') can create a repl session with options
//      - node -p will evaluate a script
//      - node -r will require a module
